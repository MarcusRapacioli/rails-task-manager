<h1>My Tasks</h1>
<% @tasks.each do | task | %>
<%= task.completed ? "[x]" : "[ ]" %>
<%= link_to "#{task.title}", task_path(task.id) %> <br>
<% end %>
<p> <%= link_to "Create new task", tasks_new_path %></p>
